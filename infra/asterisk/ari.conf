; =============================================================
; AEON Dial v3 — ARI Configuration
; Mirrored from /etc/asterisk/ari.conf
; =============================================================

[general]
enabled = yes
pretty = yes
allowed_origins = *

[aeon]
type = user
read_only = no
password = aeon_ari_secret_changeme
