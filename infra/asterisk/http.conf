; =============================================================
; AEON Dial v3 — HTTP server for ARI
; Mirrored from /etc/asterisk/http.conf
; =============================================================

[general]
enabled=yes
bindaddr=127.0.0.1
bindport=8088
